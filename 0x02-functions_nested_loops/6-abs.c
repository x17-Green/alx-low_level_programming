#include "main.h"
/**
 * _abs - Calculates the absolute value of an integer
 * @n: Integer variable
 * Return: The absolute value of an integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}

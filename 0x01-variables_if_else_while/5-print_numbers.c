#include<stdio.h>

/**
 * main - the main function
 * for: input numbers
 * Return: always 0
 */

int main(void)
{
	int n;

	for (n = 0 ; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
